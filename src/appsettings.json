{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Error"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "pgCodeCamp": "Port=5432;host=localhost;database=CodeCamp;username=postgres;",
    "pgPort": 5432,
    "pgHost": "localhost",
    "pgDb": "CodeCamp",
    "pgUser": "postgres"
  },
  "Secrets": {
    "CodeCamp": "Set in user secrets"
  }
}
